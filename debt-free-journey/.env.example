# SQLite database (relative to prisma/ directory)
DATABASE_URL="file:./dev.db"

# Optional: enables the AI fallback parser for unrecognized statement layouts.
# Leave unset to run fully offline with rule-based parsing only.
# ANTHROPIC_API_KEY=sk-ant-...
